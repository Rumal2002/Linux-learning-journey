# 📝 Day 16 Notes

## What is Docker?

Docker is an open-source platform used to develop, package, ship, and run applications inside lightweight containers.

---

## What is Containerization?

Containerization is the process of packaging an application together with all its dependencies so it can run consistently across different environments.

---

## Docker Image

A Docker Image is a read-only template used to create containers.

Example:

Image → Container

---

## Docker Container

A container is a running instance of a Docker image.

---

## Docker Hub

Docker Hub is the official online repository used to store and distribute Docker images.

---

## Docker Engine

Docker Engine is the core component responsible for building and running containers.

---

## Docker CLI

The Docker CLI allows users to interact with Docker using terminal commands.

Example:

- docker run
- docker images
- docker ps

---

## Docker vs Virtual Machine

Docker Containers

- Lightweight
- Faster startup
- Share the host kernel
- Lower resource usage

Virtual Machines

- Full operating system
- Larger storage requirements
- Higher memory consumption
- Slower startup

---

## Why Docker?

Docker helps developers eliminate the "It works on my machine" problem by providing consistent execution environments across development, testing, and production.

---

## Summary

Day 16 introduced the fundamentals of Docker and containerization. I successfully installed Docker on Ubuntu, verified the installation, and executed my first Docker container using the official `hello-world` image.
