docker login

docker logout

docker images

docker tag

docker push

docker pull

docker search

docker inspect

docker history

docker image ls

docker image rm

docker run

docker stop

docker rm

docker image prune

docker system df
